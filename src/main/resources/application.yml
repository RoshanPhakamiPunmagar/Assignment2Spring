server:
  port: 8181

spring:
  application:
    name: screamer

  datasource:
    url: jdbc:mysql://localhost:3306/screamer
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true